name: CaseMesh AWS OIDC Validate

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Type VALIDATE_AWS to confirm AWS OIDC validation'
        required: true
        type: string

permissions:
  contents: read
  id-token: write

concurrency:
  group: casemesh-aws-oidc-validate
  cancel-in-progress: false

jobs:
  validate:
    name: Validate GitHub OIDC to AWS
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Require main branch and confirmation
        shell: bash
        env:
          VALIDATE_CONFIRMATION: ${{ inputs.confirm }}
        run: |
          set -euo pipefail

          test "${GITHUB_REF}" = "refs/heads/main"
          test "${VALIDATE_CONFIRMATION}" = "VALIDATE_AWS"

      - name: Validate non-secret AWS configuration
        shell: bash
        env:
          CASEMESH_AWS_ROLE_ARN: ${{ vars.AWS_GITHUB_OIDC_ROLE_ARN }}
          CASEMESH_AWS_REGION: ${{ vars.AWS_AI_REGION }}
        run: |
          set -euo pipefail

          test -n "${CASEMESH_AWS_ROLE_ARN}"
          test -n "${CASEMESH_AWS_REGION}"

          case "${CASEMESH_AWS_ROLE_ARN}" in
            arn:aws:iam::*:role/*)
              ;;
            *)
              echo "AWS_GITHUB_OIDC_ROLE_ARN must be a standard AWS IAM role ARN."
              exit 1
              ;;
          esac

          if ! printf '%s' "${CASEMESH_AWS_REGION}" \
            | grep -Eq '^[a-z]{2}(-gov)?-[a-z0-9-]+-[0-9]+$'; then
            echo "AWS_AI_REGION has an invalid region format."
            exit 1
          fi

      - name: Configure AWS credentials with GitHub OIDC
        uses: aws-actions/configure-aws-credentials@cbe3b392738ccf3f987d68400dafcf4b0624a56c # v6.2.4
        with:
          role-to-assume: ${{ vars.AWS_GITHUB_OIDC_ROLE_ARN }}
          aws-region: ${{ vars.AWS_AI_REGION }}
          audience: sts.amazonaws.com
          role-session-name: casemesh-github-${{ github.run_id }}
          role-duration-seconds: 900
          disable-retry: true
          mask-aws-account-id: true
          unset-current-credentials: true

      - name: Validate assumed AWS identity
        shell: bash
        env:
          EXPECTED_ROLE_ARN: ${{ vars.AWS_GITHUB_OIDC_ROLE_ARN }}
        run: |
          set -euo pipefail

          read -r caller_account caller_arn < <(
            aws sts get-caller-identity \
              --query '[Account,Arn]' \
              --output text
          )

          expected_account="$(
            printf '%s' "${EXPECTED_ROLE_ARN}" \
              | cut -d: -f5
          )"

          test -n "${caller_account}"
          test -n "${caller_arn}"
          test -n "${expected_account}"

          test "${caller_account}" = "${expected_account}"

          expected_prefix="arn:aws:sts::${expected_account}:assumed-role/"

          case "${caller_arn}" in
            "${expected_prefix}"*)
              ;;
            *)
              echo "AWS caller identity is not an assumed-role session."
              exit 1
              ;;
          esac

          echo "AWS GitHub OIDC identity validation passed."

      - name: Validate Bedrock catalog access
        shell: bash
        env:
          CASEMESH_AWS_REGION: ${{ vars.AWS_AI_REGION }}
        run: |
          set -euo pipefail

          model_count="$(
            aws bedrock list-foundation-models \
              --region "${CASEMESH_AWS_REGION}" \
              --query 'length(modelSummaries)' \
              --output text \
              --no-cli-pager \
              --cli-connect-timeout 5 \
              --cli-read-timeout 15
          )"

          if ! printf '%s' "${model_count}" | grep -Eq '^[0-9]+$'; then
            echo "Bedrock model count must be a non-negative integer."
            exit 1
          fi

          if [ "${model_count}" -lt 1 ]; then
            echo "Bedrock catalog returned no foundation models."
            exit 1
          fi

          echo "AWS Bedrock catalog validation passed with ${model_count} foundation models."
